You are a senior React + TypeScript frontend engineer.

I have an existing React (Vite + TypeScript) project implementing a Domain Management Assistant chat UI.

The project structure already exists and must be modified rather than recreated.

The important files include:

src/components/
- Chat.tsx
- ChatContainer.tsx
- ChatInput.tsx
- ChatMessage.tsx
- ChatbotSuggestions.tsx
- ChatbotWidget.tsx
- DomainLandingPage.tsx
- DomainWelcome.tsx
- ChatSidebar.tsx
- ClaudeSidebar.tsx
- TypingIndicator.tsx

services/
- chatApi.ts

styles/
- globals.css

App.tsx
main.tsx

Your task is to refactor the UI and session management logic according to the following requirements.

---

# 1. Remove Floating Chatbot Widget

Currently the UI uses a floating chatbot icon that opens a popup chat window.

This logic exists mainly in:

ChatbotWidget.tsx

You must:

• Remove the floating chatbot icon  
• Remove popup open/close logic  
• Remove the close button  
• Remove all state variables related to popup visibility  
• Remove event handlers controlling popup behavior  

The chatbot should no longer appear as a popup widget.

---

# 2. Convert Chat UI to Full Page Layout

The chat interface must now behave like a full page assistant panel.

The layout should be:

Navbar  
Full Page Chat Assistant  

The chat assistant must:

• Start below the navigation bar
• Fill 100% of the remaining page height
• Cover the entire body of the page
• No floating widgets
• No modal or popup behavior

The chat window should always be visible.

---

# 3. Update Layout in DomainLandingPage.tsx

Modify DomainLandingPage.tsx so that the main content renders:

ChatContainer

instead of relying on a chatbot popup.

Example structure:

PageLayout
   Navbar
   ChatContainer

Ensure the chat container fills the remaining viewport height.

Use flex layout:

height: 100vh  
display: flex  
flex-direction: column  

Chat area should use:

flex: 1  
overflow-y: auto

---

# 4. Chat UI Structure

Ensure the chat interface has the following structure:

ChatContainer

Header  
• Domain Management Assistant title  
• welcome message  

Suggestions Section  
• Domain Procurement  
• Subdomain Creation  
• Domain Transfers  
• DNS Management  
• SSL Certificates  
• Decommission Domain  

Chat Messages Area  
• Scrollable message list  
• Displays user and assistant messages  

Chat Input Area  
• Text input  
• Send button  
• Positioned at the bottom  

---

# 5. Preserve Existing Functionalities

Do NOT break any existing features.

The following must continue working:

• message sending  
• assistant responses  
• suggestion buttons  
• typing indicator  
• API integration in chatApi.ts  
• chat history rendering  

---

# 6. Session Management Refactor

Currently:
A chat session is created when the chatbot icon is clicked.

This must be changed.

New behavior:

• Create a session automatically when the chat loads
• Session ID should refresh every 6 hours
• The refresh duration must be configurable

Implement something like:

SESSION_REFRESH_INTERVAL = 6 hours

The session logic should:

• generate a sessionId
• store it in state or context
• refresh when the interval expires
• ensure API requests always use the latest sessionId

Prefer implementing this using a custom hook such as:

useChatSession()

---

# 7. Code Optimization

While refactoring:

• Remove unused code
• Remove unused imports
• Remove dead popup logic
• Simplify component structure where possible
• Avoid unnecessary re-renders
• Improve TypeScript typing

---

# 8. Code Readability

Improve the code quality:

• Use clear component names
• Keep functions small
• Extract reusable logic
• Maintain consistent formatting

---

# 9. Comment Guidelines

Improve comments but follow these rules:

• comments must be minimal and meaningful
• do NOT comment obvious lines
• explain intent where necessary
• avoid excessive comments

Example good comment:

// Refresh session periodically to prevent expired backend sessions

---

# 10. Styling

The UI theme must remain:

• white / light theme
• clean and modern
• responsive

Ensure:

• proper spacing
• clean layout
• good scroll behavior

Use existing styles from:

globals.css

Do not introduce unnecessary CSS complexity.

---

# 11. Expected Output

Modify the existing files in the project instead of generating a new project.

Show the updated versions of the files that require changes, especially:

ChatbotWidget.tsx  
ChatContainer.tsx  
Chat.tsx  
DomainLandingPage.tsx  
App.tsx  
chatApi.ts  

Ensure the final code is clean, optimized, and production-ready.